# 🌿 وكالة الشويع - نظام المحاسب الذكي (v3.0)

النظام المحاسبي الأكثر تطوراً لإدارة وكالات القات في اليمن، يجمع بين دقة العمليات المالية وذكاء المحركات البرمجية الحديثة.

## 🏗️ البنية المعمارية (Architecture)

يعتمد التطبيق على معمارية **Single Page Application (SPA)** متكاملة مع خدمات سحابية (Serverless):

### 1. الطبقة الأمامية (Frontend Layer)
- **React 19 & TypeScript**: لضمان استقرار الكود وكفاءة الأداء.
- **Context API**: إدارة الحالة الشاملة عبر `AgencyContext` و `NotificationContext`.
- **Tailwind CSS**: تصميم عصري متجاوب يدعم الوضع الليلي (Dark Mode) ونمط الخصوصية.

### 2. الطبقة الخلفية (Backend Layer - Supabase)
- **Database**: PostgreSQL لتخزين البيانات المالية مع RLS (Row Level Security).
- **Auth**: نظام تسجيل دخول مشفر وآمن.
- **Edge Functions**: معالجة المهام الثقيلة مثل المزامنة السحابية وإرسال رسائل البريد.
- **Storage**: تخزين صور السندات والوصولات.

### 3. طبقة الذكاء الاصطناعي (AI Layer)
- **Google Gemini 3.0**: يستخدم لتحليل البيانات، استخراج العمليات من الأوامر الصوتية، وتقديم نصائح تجارية لحظية.
- **Live API**: بث صوتي لحظي للتفاعل مع النظام.

---

## 📂 هيكلة الملفات (Project Structure)

```text
├── components/          # واجهات المستخدم (Dashboard, Sales, etc.)
├── context/             # إدارة الحالة العامة (Global State)
├── hooks/               # الخطافات المخصصة (useStats, useLiveAssistant)
├── services/            # خدمات خارجية (Gemini, Telegram, WhatsApp)
├── types.ts             # تعريفات الأنواع والواجهات (TypeScript Interfaces)
├── supabase/            # وظائف السيرفر (Edge Functions)
└── tests/               # ملفات الاختبار والتحقق
```

---

## 🚀 التشغيل والتطوير

1. تأكد من وجود مفتاح `API_KEY` الخاص بـ Gemini في بيئة العمل.
2. قم بضبط متغيرات Supabase: `VITE_SUPABASE_URL` و `VITE_SUPABASE_ANON_KEY`.
3. استخدم `npm run dev` للتشغيل المحلي.

## 🧪 نظام الاختبار (Testing)
تمت إضافة اختبارات وحدات (Unit Tests) للمنطق المحاسبي في مجلد `/tests` للتأكد من صحة حسابات المخزون والديون عند إجراء أي تحديثات مستقبلية.

## 🛡️ الأمان والخصوصية
- تشفير كامل للبيانات عند الانتقال.
- وضع الخصوصية (Privacy Mode) لإخفاء الأرقام الحساسة أثناء الاستخدام العام.
- عزل بيانات كل مستخدم عن الآخر بشكل كامل عبر سياسات PostgreSQL.

---
*تم تطويره بواسطة فريق الشويع للحلول الذكية ✨*
